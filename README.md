# my-playwright-cucumber-ts-ui
This is Playwright project with Cucumber and typescript

to execute scenarios run below commands
`npx playwright install --with-deps chromium`

`npm run test --tags='@amazon'`

